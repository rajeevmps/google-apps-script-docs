# Avoid

An enum representing the types of restrictions to avoid when finding directions.

An enum representing the types of restrictions to avoid when finding directions. To call an enum, use the syntax `Maps.Avoid.TOLLS` or `Maps.Avoid.HIGHWAYS`.

## Properties (Enum Values)

| Property | Description |
|----------|--------------|
| `TOLLS` | Avoid tolls. |
| `HIGHWAYS` | Avoid highways. |
