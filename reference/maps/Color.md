# Color

An enum representing the named colors available to use in map images.

An enum representing the named colors available to use in map images. To reference a color, use the syntax `Maps.Color.BLACK` (calling the parent class, name, and property).

## Properties (Enum Values)

| Property |
|----------|
| `BLACK` |
| `BROWN` |
| `GREEN` |
| `PURPLE` |
| `YELLOW` |
| `BLUE` |
| `GRAY` |
| `ORANGE` |
| `RED` |
| `WHITE` |

Note: the reference page lists these ten color options but does not provide individual descriptions for each enum value beyond their names.
