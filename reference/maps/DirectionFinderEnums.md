# DirectionFinderEnums

A collection of enums used by DirectionFinder.

A collection of enums used by `DirectionFinder`.

## Properties

| Property | Type |
|----------|------|
| `Avoid` | `Avoid` |
| `Mode` | `Mode` |

See the [Avoid](./Avoid.md) and [Mode](./Mode.md) pages for the full list of enum values.
