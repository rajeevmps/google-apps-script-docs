# ColumnType

An enumeration of the valid data types for columns in a DataTable.

An enumeration of the valid data types for columns in a `DataTable`. To call an enum, you use this pattern: `Charts.ColumnType.DATE`.

## Methods

None.

## Properties

| Property | Type | Description |
|---|---|---|
| DATE | Enum | Corresponds to date values. |
| NUMBER | Enum | Corresponds to number values. |
| STRING | Enum | Corresponds to string values. |
