# AutoTextType

The types of auto text.

The types of auto text. To reference this enum, use `SlidesApp.AutoTextType.SLIDE_NUMBER`.

## Values

- `UNSUPPORTED` — An auto text type that is not supported.
- `SLIDE_NUMBER` — A slide number.
