# LineFillType

The kinds of line fill.

The kinds of line fill. This enum is accessed via its parent class: `SlidesApp.LineFillType`.

## Values

- `UNSUPPORTED` — A line fill type that is not supported.
- `NONE` — No fill, so the line or outline is transparent.
- `SOLID` — A solid color fill.
