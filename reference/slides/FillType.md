# FillType

The kinds of fill.

The kinds of fill. To access these values, use `SlidesApp.FillType.SOLID`.

## Values

- `UNSUPPORTED` — A fill type that is not supported.
- `NONE` — No fill, so the background is transparent.
- `SOLID` — A solid color fill.
