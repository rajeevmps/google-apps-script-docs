# BigQueryParameterType

An enum that defines the BigQuery parameter types that you can set.

An enum that defines the BigQuery parameter types that you can set. Access via `DataStudioApp.BigQueryParameterType.[PROPERTY]`.

## Properties (Enum Values)

| Property | Description |
|----------|--------------|
| `STRING` | String. |
| `INT64` | 64-bit integer. |
| `BOOL` | Boolean. |
| `FLOAT64` | 64-bit floating point number. |
