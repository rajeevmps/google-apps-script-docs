# SendStatus

An enum value that specifies the send status of an `UpdateDraftActionResponse`.

An enum value that specifies the send status of an `UpdateDraftActionResponse`.

## Properties

| Property | Description |
|----------|-------------|
| `NOT_SEND` | Default. Don't send the email after updating the draft. |
| `CONTINUE_SEND` | Continue sending the email after updating the draft. |
