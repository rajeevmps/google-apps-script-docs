# ValidationErrorSeverity

An enum that represents the severity of the validation error.

An enum that represents the severity of the validation error.

## Properties

| Property | Description |
|----------|-------------|
| `SEVERITY_UNSPECIFIED` | Unspecified. |
| `CRITICAL` | Critical, the error is critical and must be fixed before publish. |
| `WARNING` | Warning, the error is non-critical and is not blocking publish. |
