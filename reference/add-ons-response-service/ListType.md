# ListType

An enum that represents the type of the list.

An enum that represents the type of the list.

## Properties

| Property | Description |
|----------|-------------|
| `LIST_TYPE_UNSPECIFIED` | Default value. Don't use. |
| `ORDERED` | Ordered list. |
| `UNORDERED` | Unordered list. |
