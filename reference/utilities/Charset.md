# Charset

A typesafe enum for character sets.

A typesafe enum for character sets. To call an enum, you call its parent class, name, and property. For example, `Utilities.Charset.US_ASCII`.

## Methods

None.

## Properties

| Property | Type | Description |
|----------|------|-------------|
| `US_ASCII` | Enum | US ASCII charset. |
| `UTF_8` | Enum | UTF-8 charset. |

### US_ASCII

US ASCII charset.

### UTF_8

UTF-8 charset.
